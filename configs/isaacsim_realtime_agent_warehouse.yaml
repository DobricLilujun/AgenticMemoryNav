runtime:
  output_root: outputs
  max_steps: 20
  instruction: "Find the red cup in the kitchen"
mapping:
  backend: mock
  keyframe_interval: 1
perception:
  backend: vlm
  model_id: Inferact/Qwen3.8-27B-NVFP4
  base_url: http://10.6.32.16:8000/v1
  api_key: dummy
  api: openai-completions
  timeout: 90.0
execution:
  # NVIDIA SimReady environment, served from the official Isaac Sim asset CDN
  # (Window > Browsers > NVIDIA Assets > Warehouse in the interactive editor).
  scene: https://omniverse-content-production.s3-us-west-2.amazonaws.com/Assets/Isaac/6.0/Isaac/Environments/Simple_Warehouse/warehouse.usd
  max_speed: 0.5
  max_angular_speed: 1.0
  max_action_timeout: 15.0
  headless: true
  camera_width: 256
  camera_height: 192
  waypoint_duration_s: 4.0
