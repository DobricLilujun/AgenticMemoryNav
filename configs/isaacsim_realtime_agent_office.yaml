runtime:
  output_root: outputs
  max_steps: 40
  instruction: "Find the red cup on the office desk"
mapping:
  backend: mock
  keyframe_interval: 1
perception:
  backend: vlm
  model_id: Inferact/Qwen3.8-27B-NVFP4
  base_url: http://10.6.32.16:8000/v1
  api_key: dummy
  api: openai-completions
  timeout: 120.0
execution:
  # More complex indoor office environment with desks, partitions, and multiple
  # navigable corridors; better suited for long-horizon room-to-room search.
  scene: https://omniverse-content-production.s3-us-west-2.amazonaws.com/Assets/Isaac/6.0/Isaac/Environments/Simple_Office/office.usd
  max_speed: 0.4
  max_angular_speed: 0.9
  max_action_timeout: 20.0
  headless: true
  camera_width: 320
  camera_height: 240
  waypoint_duration_s: 5.0
