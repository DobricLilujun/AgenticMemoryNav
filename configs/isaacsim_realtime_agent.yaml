runtime:
  output_root: outputs
  max_steps: 40
  instruction: "Find the red cup in the kitchen"
mapping:
  backend: mock
  keyframe_interval: 1
perception:
  backend: mock
  # Real VLM example (requires network access to the endpoint):
  # backend: vlm
  # model_id: Inferact/Qwen3.8-27B-NVFP4
  # base_url: http://10.6.32.16:8000/v1
  # api_key: dummy
  # timeout: 90.0
execution:
  max_speed: 0.5
  max_angular_speed: 1.0
  max_action_timeout: 15.0
  headless: true
  camera_width: 256
  camera_height: 192
  waypoint_duration_s: 4.0
