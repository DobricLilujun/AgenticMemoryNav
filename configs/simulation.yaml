runtime:
  output_root: outputs
  queue_size: 32
  max_frames: 100
mapping:
  backend: mock
perception:
  backend: mock
planning:
  backend: rule_based
execution:
  backend: habitat
  real_robot_enabled: false
  scene: null